# Implementation Gauntlet Progress

## Feature: Add CI-only checks to Makefile so `make check` runs everything CI runs

Goal: `make check` (Makefile:50) already covers the `checks` job of
`.github/workflows/ci.yml` plus extras. Three CI checks are missing and must be
added as Makefile targets and wired into `check`:

1. `check-prod-tags` — mutable-tag guard (ci.yml:82-92): grep
   `deploy/flux/overlays/production/` for `tag:.*latest` and `tag:.*"main"` in
   `*.yaml`; fail if found.
2. `docker-check` — build (NOT push) both images: core via
   `scripts/docker-build-push.sh --build-only`, MCP via the equivalent for
   `Dockerfile.mcp`.
3. `trivy-check` — scan both built images CRITICAL,HIGH, ignore-unfixed
   (build-core.yml:66-73, build-mcp.yml:65-72). Fail clearly if trivy absent
   unless `SKIP_TRIVY=1`.

Constraints:
- Update `.PHONY` for new targets.
- `check` order: cheap checks first, docker last.
- Do NOT change CI workflows; Makefile is the source of local parity.
- Verify: `make check` locally + each new target in isolation.

Approach: parameterize `scripts/docker-build-push.sh` with `DOCKERFILE` and
`LOCAL_TAG` env vars (defaults preserve current behavior) so the same script
builds both images with distinct local tags (`commoncal:local` core,
`commoncal-mcp:local` MCP). Add 3 Makefile targets + wire into `check` + extend
`.PHONY` (also fixing latent missing `sqlite-prod-test`).

SME: `david-hajnal <david@hajnal.space>` (sole author; owns Makefile + build scripts).

### Task 1: Parameterize docker-build-push.sh (DOCKERFILE + LOCAL_TAG)
- **ID:** T01
- **Name:** parameterize-docker-build-push
- **Status:** DONE
- **Dependencies:** none
- **Files:** `scripts/docker-build-push.sh` (`LOCAL_TAG` :84, `DOCKERFILE` default :74, `build_local` :121, `build_push` :138, `usage()` + header env list)
- **Acceptance:** `DOCKERFILE` (default `Dockerfile`) + `LOCAL_TAG` (default `commoncal:local`) overridable via env; defaults preserve current behavior.
- **Verification:** `bash -n` PASS; `--dry-run` (core) byte-identical to original (PASS); MCP `--dry-run` lists `calendar-mcp`/`commoncal-mcp:local` (PASS). Real builds deferred to T01b (blocked by pre-existing tag bug).
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — diff shows only in-scope lines (DOCKERFILE default, LOCAL_TAG overridable, `-f "${DOCKERFILE}"`, usage docs); defaults preserved.

### Task 1b: Fix pre-existing tag-expansion bug (blocker for all local builds)
- **ID:** T01b
- **Name:** fix-tag-expansion
- **Status:** DONE
- **Dependencies:** T01
- **Files:** `scripts/docker-build-push.sh` (`build_local` :113-127, `build_push` :134-149)
- **Root cause:** `"${TAGS[@]/#/-t }"` yields single words like `-t <tag>` (embedded space); docker rejects `invalid tag " <tag>"`. Introduced in `e5c1652`. Broke ALL local builds (existing `make docker-build`/`docker-push`/`docker-build-push` + new `docker-check`).
- **Acceptance:** `build_local`/`build_push` pass `-t <tag>` as SEPARATE args; core real build → `commoncal:local` (port 3000); MCP real build → `commoncal-mcp:local` (port 3001); two images distinct.
- **Verification:** core `--build-only` SUCCESS + `commoncal:local` 3000/tcp; MCP `--build-only` SUCCESS + `commoncal-mcp:local` 3001/tcp; distinct IDs (e3de1203 vs 803f7d43).
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — tag_args loop in both funcs; real builds succeeded; distinct images.

> **CONCURRENT-COMMIT NOTE:** While this run was in flight, the SME (david-hajnal)
> committed in parallel on the same area: `45083fc` (the T01+T01b script changes —
> now in HEAD, working tree matches HEAD), `1a68246` + `0660119` (CI fixes: GHCR
> login via GITHUB_TOKEN, Trivy pinned v0.36.0 with `image-ref`, severity
> CRITICAL,HIGH / exit-code 1 / ignore-unfixed — unchanged parity flags). The
> Makefile targets (T02-T05) remain pending and are the remaining core work.

### Task 2: Add check-prod-tags target
- **ID:** T02
- **Name:** add-check-prod-tags
- **Status:** DONE
- **Dependencies:** none
- **Files:** `Makefile` (new target)
- **Acceptance:** `make check-prod-tags` exits 0 on clean tree; fails (exit 1 + ERROR) when a `tag: latest` / `tag: "main"` line is injected into a prod `.yaml`. Mirrors ci.yml:82-92 wording.
- **Verification:** `make check-prod-tags` PASS (exit 0); inject `tag: latest` -> FAIL (ERROR 'latest'); rm -> PASS.
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — recipe mirrors ci.yml:82-92 (same grep patterns/messages/include); clean pass + negative inject verified by orchestrator.

### Task 3: Add docker-check target
- **ID:** T03
- **Name:** add-docker-check
- **Status:** DONE
- **Dependencies:** T01
- **Files:** `Makefile` (new target)
- **Acceptance:** `make docker-check` builds both `commoncal:local` (core) and `commoncal-mcp:local` (MCP); no push/login.
- **Verification:** `make docker-check` PASS; `docker image ls` shows both `commoncal:local` + `commoncal-mcp:local`.
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — core (3000) + MCP (3001) built; distinct; no login/push in recipe.

### Task 4: Add trivy-check target
- **ID:** T04
- **Name:** add-trivy-check
- **Status:** DONE
- **Dependencies:** T01, T03
- **Files:** `Makefile` (new target)
- **Acceptance:** scans both distinct images CRITICAL,HIGH ignore-unfixed exit-code 1; trivy absent + no SKIP_TRIVY → clear ERROR exit 1; trivy absent + SKIP_TRIVY=1 → skip exit 0. Prereq `docker-check` so it works in isolation.
- **Verification:** `make trivy-check` (trivy present); `PATH=/usr/bin:/bin make trivy-check` (ERROR exit 1); `PATH=/usr/bin:/bin SKIP_TRIVY=1 make trivy-check` (skip exit 0).
- **Worker confidence:** —
- **Orchestrator acceptance:** —

### Task 5: Wire into check + update .PHONY
- **ID:** T05
- **Name:** wire-check-and-phony
- **Status:** DONE
- **Dependencies:** T02, T03, T04
- **Files:** `Makefile:1` (.PHONY), `Makefile:50` (check)
- **Acceptance:** `.PHONY` gains `check-prod-tags`, `docker-check`, `trivy-check`, `sqlite-prod-test` (alphabetical after `all`); `check` = cheap-first order ending `... docker-check trivy-check`.
- **Verification:** `make -n check` order confirmed (check-prod-tags early; docker-check+trivy-check last); no make syntax errors; all 5 targets in .PHONY.
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — .PHONY alphabetical incl. sqlite-prod-test (latent bug fixed); check order cheap-first/docker-last confirmed by orchestrator.

## Verification:
- `make -n check` — PASS — order: check-prod-tags (3rd, cheap); docker-check + trivy-check (last two).
- `make check-prod-tags` — PASS (clean, exit 0); inject `tag: latest` → FAIL (ERROR); inject `tag: "main"` → FAIL (ERROR); rm → PASS.
- `make docker-check` — PASS — both `commoncal:local` (3000/tcp) + `commoncal-mcp:local` (3001/tcp) built, distinct IDs.
- `make trivy-check` — PASS — scanned BOTH images, 0 findings (CRITICAL,HIGH, ignore-unfixed, exit-code 1).
- `env PATH=/usr/bin:/bin:/usr/local/bin make trivy-check` — FAIL (trivy-absent ERROR, exit 1); `SKIP_TRIVY=1` → skip (exit 0).
- `cargo check --locked` (backend + mcp) — PASS; `make backend-check`/`mcp-check`/`backend-test` — PASS.
- `make -n deploy` — PASS — prints `deploy/deploy-prod.sh` (no longer a no-op).
- `scripts/docker-build-push.sh --dry-run` — PASS — defaults intact (`calendar-core` + `commoncal:local`).
- `make check` (full) — all steps green through frontend-build; stops at PRE-EXISTING `deploy-script-test` (KUBECONFIG) — not a defect of this change.

## QA Verdict:
- **PASS** (fresh QA, applies to final diff). All 7 ACs verified; R01 (Cargo.lock) + R02 (deploy .PHONY) confirmed; no defects. Pre-existing concerns (KUBECONFIG, pnpm-install gap, mcp dead-code warnings) confirmed NOT from this change.

## Gauntlet:
### Round 1 (3 lanes: Correctness, Architecture/CI-parity, Security) — FAIL
- Correctness: PASS — 0 BLOCKER/HIGH, 3 LOW (all CI-inherited/spec-literal: `latest` false-positive on version strings; `tag: main` unquoted false-negative; SKIP_TRIVY literal-`1` only).
- Architecture/CI-parity: FAIL — 1 BLOCKER (F1: `make check`/`docker-check` red at HEAD — `5db4df4` bumped Cargo.toml to 2.1.0 but left both Cargo.lock at 2.0.0, so `--locked` fails; reproduced `cargo check --locked` exit 101), 1 HIGH (F2: `make deploy` silent no-op — `deploy` missing from .PHONY, shadowed by `deploy/` dir; `make -n deploy` → "up to date"), 1 MEDIUM (F3: `pnpm install --frozen-lockfile` parity gap), 1 LOW (F4: trivy version drift local 0.74.0 vs CI v0.36.0).
- Security: PASS WITH CONCERNS — 0 BLOCKER/HIGH, 1 MEDIUM (F1: unquoted `tag: main` bypasses guard — CI-identical ci.yml:92; backstopped by Flux image-automation), 1 LOW (F2: guard scope `--include='*.yaml'` + prod-overlay-only; mcp chart default path).

### Remediation R01 (BLOCKER: stale Cargo.lock) — DONE
- `backend/Cargo.lock:296` commoncal-backend `2.0.0`→`2.1.0`; `mcp-server/Cargo.lock:1035` mcp-server `2.0.0`→`2.1.0`. Precedent: `c9345fa` (same fix for 2.0.0). Pre-existing (user's `5db4df4` bump), not caused by this change.
- Verify: `cargo check --locked` PASS (both); `make backend-test` PASS; `make docker-check` PASS (both images build).

### Remediation R02 (HIGH: `make deploy` no-op) — DONE
- `Makefile:1` .PHONY: added standalone `deploy` (between `ci-script-test` and `deploy-script-test`). Pre-existing bug; fixed since this diff already edits .PHONY.
- Verify: `make -n deploy` → `deploy/deploy-prod.sh` (recipe runs, not "up to date").

### Accepted residual concerns (with rationale)
- MEDIUM (Arch F3): `pnpm install --frozen-lockfile` has no Makefile target. OUT OF TASK SCOPE (task explicitly scoped to the 3 listed checks); pre-existing; adding it is a separate change with side effects.
- MEDIUM (Sec F1): unquoted `tag: main` bypasses `check-prod-tags`. FAITHFUL MIRROR of ci.yml:92 (task requires exact CI parity — "Makefile is the source of local parity"); pre-existing in CI; backstopped by Flux ImageUpdateAutomation (reverts to semver within 2m).
- LOW (Arch F4): trivy version drift (local 0.74.0 vs CI action v0.36.0) — inherent to local parity; informational.
- LOW (Sec F2): guard scope `--include='*.yaml'` + prod-overlay-only — same scope as CI; low exploitability.
- LOW (Correctness x3): grep edge cases + SKIP_TRIVY literal — all CI-inherited/spec-literal.

### Round 2 (3 fresh lanes: Architecture/CI-parity, Correctness, Security) — CLEAN (no BLOCKER/HIGH)
- Architecture/CI-parity: PASS WITH CONCERNS — 0 BLOCKER/HIGH, 1 LOW (trivy-check hard-couples to docker-check; SKIP_TRIVY escapes the scan but not the build — defensible: scan needs artifacts, AC1 mandates build in `check`). R01+R02 CONFIRMED FIXED, no regression (clean non-cached docker build re-ran `cargo build --release --locked` OK).
- Correctness: PASS WITH CONCERNS — 0 BLOCKER/HIGH, 1 LOW (same trivy-check build-on-skip). `$$`→`$` SKIP_TRIVY correct; `&&` propagation correct; tag_args loop correct for all 4 tags; Cargo.lock R01 = exactly 2 version lines; `deploy` .PHONY R02 no shadowing.
- Security: PASS — 0 BLOCKER/HIGH, 3 LOW (all pre-existing/design-inherent: guard fail-open on missing overlay dir [CI-identical]; `IMAGE_TAG=main` default in deploy/.env.example [pre-existing, outside guard scope]; trivy covers OS layer only, Rust crate CVEs invisible [design-inherent, identical in CI]).
- **Round 2 is CLEAN: 0 BLOCKER, 0 HIGH across all 3 lanes.**

### Gauntlet FINAL STATE
- 2 rounds executed; 3 lanes each round (Architecture/CI-parity, Correctness, Security).
- Round 1: FAIL (1 BLOCKER stale Cargo.lock + 1 HIGH make-deploy no-op) → remediated (R01, R02).
- Round 2: CLEAN (0 BLOCKER/HIGH; only LOWs, all defensible/pre-existing/design-inherent).
- Exit conditions met: required tests pass on current diff; QA PASS; all ACs satisfied; 0 BLOCKER; 0 HIGH; no unresolved defect invalidates behavior; all 3 lanes PASS/PASS WITH CONCERNS in final round.
- Rejected findings: none (all accepted findings had concrete evidence/plausible path).
- NOTE: user committed `e12d6e6` (Cargo.lock sync = R01, + `cargo update -w` in release.sh as preventive) mid-review; Makefile changes remain the uncommitted deliverable.

## Residual Concerns (accepted LOWs / follow-ups):
- LOW: `trivy-check: docker-check` builds images even on the SKIP_TRIVY/error path (defensible: scan needs artifacts; AC1 mandates build in `check`). Follow-up: optional `trivy-scan` target that assumes images exist.
- LOW: `check-prod-tags` fail-open if `deploy/flux/overlays/production/` is absent (CI-identical; a missing prod overlay breaks deployment itself).
- LOW: `deploy/.env.example:7` `IMAGE_TAG=main` default (pre-existing, outside guard scope; currently fails closed until a `main` tag is pushed).
- LOW: trivy scan covers OS layer only; Rust crate CVEs invisible (design-inherent, identical in CI; crate-level `cargo audit` is a separate control).
- MEDIUM (accepted, out of scope): `pnpm install --frozen-lockfile` has no Makefile target (ci.yml:60) — pre-existing parity gap; task explicitly scoped to the 3 listed checks.
- MEDIUM (accepted, CI-inherited): unquoted `tag: main` bypasses `check-prod-tags` (faithful mirror of ci.yml:92; backstopped by Flux ImageUpdateAutomation).
- PRE-EXISTING (unrelated, needs separate ticket): `make deploy-script-test` fails on a host without KUBECONFIG (deploy-prod.sh:74) — blocks full `make check` off the k3s host.

## Implementation Confidence:
HIGH — all 7 acceptance criteria verified (final QA PASS); 3 new targets work in isolation (check-prod-tags clean+negative, docker-check both images, trivy-check both scans + SKIP_TRIVY/absent branches); gauntlet 2 rounds with final round CLEAN (0 BLOCKER/HIGH); round-1 BLOCKER (stale Cargo.lock) + HIGH (make-deploy no-op) found, proven, fixed, re-verified; `--locked` builds + docker builds + trivy scans all green.

## SMEs:
- `david-hajnal <david@hajnal.space>` — Makefile, scripts/, build workflows, CI (sole author). Committed in parallel: `45083fc` (script tag-fix + DOCKERFILE/LOCAL_TAG), `1a68246`/`0660119` (CI GHCR/Trivy fixes), `e12d6e6` (Cargo.lock sync + release.sh `cargo update -w`).

## Docs:
- `README.md:137` — added `DOCKERFILE`, `LOCAL_TAG` to build-script env-var list (script now honors both).
- `README.md:139-142` — note that `make check` runs `docker-check` (core+MCP local builds) + `trivy-check` (CRITICAL/HIGH scan, `SKIP_TRIVY=1` to skip).
- `check-prod-tags` not added to README — already covered by `docs/DEPLOYMENT.md:159-164` ("No mutable tags (latest/main) in production").
- No CHANGELOG in repo (n/a).

## Commit Message:
```
build(make): add CI-parity checks (prod-tags, docker, trivy) to `make check`

`make check` now runs the three CI checks that were missing locally:
- check-prod-tags: guard against mutable `latest`/`main` image tags in
  deploy/flux/overlays/production/ (mirrors ci.yml deploy-validation).
- docker-check: build (not push) the core and MCP images.
- trivy-check: scan both images for CRITICAL/HIGH (ignore-unfixed);
  SKIP_TRIVY=1 to skip; clear error if trivy is absent.

Also:
- parameterize scripts/docker-build-push.sh with DOCKERFILE + LOCAL_TAG
  (defaults preserve existing behavior) so the same script builds both
  images under distinct local tags; fix the tag-args expansion that glued
  `-t` and the tag into one word and broke every local build.
- add the missing `sqlite-prod-test` and `deploy` to .PHONY (deploy was a
  silent no-op shadowed by the deploy/ dir).

Order in `check`: cheap checks first, docker + trivy last. CI workflows
unchanged; Makefile is the source of local parity.
```

## PR Message:
```
## Summary
Adds the three CI-only checks that were missing from `make check` so local
parity matches CI: a production mutable-tag guard, a build-only Docker check
for both images, and a Trivy vulnerability scan.

## Changes
- `Makefile` — new targets `check-prod-tags` (mirrors ci.yml deploy-validation
  guard), `docker-check` (build core `commoncal:local` + MCP
  `commoncal-mcp:local`, no push), `trivy-check` (scan both,
  CRITICAL/HIGH, ignore-unfixed, exit-code 1; `SKIP_TRIVY=1` escape; clear
  error if trivy absent). Wired into `check` (cheap-first / docker-last) and
  `.PHONY`. Also added the missing `sqlite-prod-test` and `deploy` to `.PHONY`
  (`deploy` was a silent no-op shadowed by the `deploy/` directory).
- `scripts/docker-build-push.sh` — honor `DOCKERFILE` + `LOCAL_TAG` env vars
  (defaults preserve current behavior) so one script builds both images under
  distinct local tags; fix the `"${TAGS[@]/#/-t }"` expansion that glued `-t`
  and the tag into a single word and broke every local `docker buildx` call.
- `README.md` — document the new `DOCKERFILE`/`LOCAL_TAG` env vars and that
  `make check` now runs the docker + trivy checks.
- (separate, already committed) `Cargo.lock` sync to 2.1.0 + `release.sh`
  `cargo update -w` so version bumps keep `--locked` builds green.

## Test Plan
- `make check-prod-tags` — passes clean; fails (exit 1) on injected
  `tag: latest` / `tag: "main"`; passes after removal.
- `make docker-check` — builds both images (`commoncal:local` 3000/tcp,
  `commoncal-mcp:local` 3001/tcp), distinct IDs, no push/login.
- `make trivy-check` — scans both images (0 findings); trivy-absent → clear
  error; `SKIP_TRIVY=1` → skip.
- `cargo check --locked` (backend + mcp), `make backend-test` — green.
- `make -n deploy` — runs `deploy/deploy-prod.sh` (no longer a no-op).
- `make -n check` — ordering cheap-first / docker-last.

## SMEs
- @david-hajnal — Makefile, scripts/, CI workflows (sole author).

## Notes
- Pre-existing, unrelated: `make deploy-script-test` requires KUBECONFIG
  (deploy-prod.sh:74), so a full `make check` fails off the k3s host at that
  step; all steps before it (and the three new targets) pass.
- Accepted residuals (CI-inherited / design-inherent, tracked as follow-ups):
  unquoted `tag: main` not caught (faithful mirror of ci.yml:92, backstopped
  by Flux image-automation); trivy scan covers the OS layer only; `trivy-check`
  builds images even on the skip path (scan needs the artifacts).
```
