# Implementation Gauntlet Progress

## Feature: Local Docker Build & Push to GHCR

### Task 1: docker-build-push.sh
- **ID:** T1
- **Name:** docker-build-push-script
- **Status:** DONE
- **Dependencies:** none
- **Files:** `scripts/docker-build-push.sh` (new)
- **Verification:** `bash scripts/docker-build-push.sh --help` — exits 0; `bash scripts/docker-build-push.sh --dry-run` — prints commands without executing
- **Assumptions:** GHCR repo = `ghcr.io/david-hajnal/calendar-core`; IMAGE_TAG defaults to git short SHA; GHCR_TOKEN from env or `gh auth token`
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — syntax clean, executable, help/dry-run flags work

### Task 2: Makefile docker targets
- **ID:** T2
- **Name:** makefile-docker-targets
- **Status:** DONE
- **Dependencies:** T1
- **Files:** `Makefile`, `scripts/docker-build-push.sh` (added --build-only)
- **Verification:** `make -n docker-build` — PASS; `make -n docker-push` — PASS; `make -n docker-build-push` — PASS
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — all 3 targets verified, IMAGE_TAG override works

### Task 3: GitHub Actions CI workflow
- **ID:** T3
- **Name:** github-actions-ci
- **Status:** DONE
- **Dependencies:** none
- **Files:** `.github/workflows/ci.yml` (new)
- **Verification:** YAML valid; triggers on push/PR to main; docker job runs on main push only
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — valid YAML, correct triggers, GHCR auth configured

### Task 4: README update
- **ID:** T4
- **Name:** readme-update
- **Status:** DONE
- **Dependencies:** T1, T2
- **Files:** `README.md`
- **Verification:** new "Local Docker build" section present; existing content preserved; no duplicates
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — sections added, content preserved, formatting consistent
- **Dependencies:** T1, T2
- **Files:** `README.md`
- **Verification:** new build/push instructions present

### Task 5: .env.local GHCR_TOKEN
- **ID:** T5
- **Name:** env-local-ghcr-token
- **Status:** DONE
- **Dependencies:** none
- **Files:** `.env.local`, `.env.example`
- **Verification:** `grep GHCR_TOKEN .env.local .env.example` — both contain GHCR_TOKEN
- **Worker confidence:** HIGH
- **Orchestrator acceptance:** PASS — placeholder added to both files, comments present
- **Dependencies:** none
- **Files:** `.env.local`
- **Verification:** GHCR_TOKEN present

## Verification:
- `bash scripts/docker-build-push.sh --help` — exits 0
- `GHCR_TOKEN=fake bash scripts/docker-build-push.sh --dry-run` — prints commands, exits 0
- `bash -n scripts/docker-build-push.sh` — syntax OK
- `test -x scripts/docker-build-push.sh` — executable
- `make -n docker-build` — `scripts/docker-build-push.sh --build-only`
- `make -n docker-push` — `scripts/docker-build-push.sh`
- `make -n docker-build-push` — `scripts/docker-build-push.sh`
- `make -n docker-build IMAGE_TAG=v1.2.3` — IMAGE_TAG passed via env
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` — YAML valid
- `grep GHCR_TOKEN .env.local .env.example` — both contain GHCR_TOKEN
- `grep -c '## Local Docker build' README.md` — 1 (no duplicates)

## Gauntlet:
- Round 1: PASS WITH CONCERNS — 5 HIGH (fixed 2: BUILD_ONLY init, docker logout trap), 4 MEDIUM, 3 LOW, 2 rejected
- Round 2: PASS — 1 HIGH rejected (GHCR_TOKEN is correct convention), 1 MEDIUM, 3 LOW (non-blocking)

## Residual Concerns:
- MEDIUM: dry-run output conflates build/tag lines (cosmetic)
- LOW: `docker-build-push` and `docker-push` targets are identical
- LOW: no Dockerfile existence pre-check
- LOW: `--dirty` flag produces non-reproducible local tags

## Implementation Confidence:
HIGH — all acceptance criteria met, both gauntlet rounds passed, syntax/dry-run verified

## SMEs:
- Docker/infra: existing deploy/ scripts, Helm charts
- CI: no prior GitHub Actions in repo (new)

## Docs:
- `README.md` — new "Local Docker build" section (lines 105-137)
- `scripts/docker-build-push.sh` — inline usage docs
- `.env.local`, `.env.example` — GHCR_TOKEN placeholder

## Commit Message:
```
feat: add local Docker build and GHCR push workflow

- scripts/docker-build-push.sh: build+push script with GHCR auth
- Makefile: docker-build, docker-push, docker-build-push targets
- .github/workflows/ci.yml: CI + GHCR push on main push
- README.md: Local Docker build documentation
- .env.local, .env.example: GHCR_TOKEN placeholder
```

## PR Message:
```
feat: add local Docker build and push to GHCR

## Summary
Adds `scripts/docker-build-push.sh` for building and pushing Docker images
to `ghcr.io/david-hajnal/calendar-core`. Includes Makefile targets, GitHub
Actions CI workflow, and documentation.

## Changes
- `scripts/docker-build-push.sh` — new: build+push with GHCR auth (env var or gh CLI fallback)
- `Makefile` — added docker-build, docker-push, docker-build-push targets
- `.github/workflows/ci.yml` — new: CI (make check) + docker push on main push
- `README.md` — new "Local Docker build" section
- `.env.local`, `.env.example` — GHCR_TOKEN placeholder

## Test Plan
- `make docker-build` — builds commoncal:local
- `make docker-push` — pushes to GHCR (requires GHCR_TOKEN)
- `make docker-build-push` — build + push
- `make docker-build IMAGE_TAG=v1.2.3` — custom tag
- CI: push to main triggers CI + docker job

## SMEs
- Infra: deploy/ scripts, Helm charts (values-production.yaml)
```
