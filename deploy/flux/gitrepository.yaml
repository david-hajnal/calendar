apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: commoncal
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/david-hajnal/calendar
  ref:
    branch: main
  ignore: |
    # exclude deploy/k3s/* from sync
    ---
    deploy/k3s/*
