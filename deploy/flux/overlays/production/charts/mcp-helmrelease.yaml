apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: commoncal-mcp
  namespace: flux-system
spec:
  releaseName: commoncal-mcp
  targetNamespace: commoncal
  chart:
    spec:
      chart: ./deploy/helm/commoncal-mcp
      sourceRef:
        kind: GitRepository
        name: flux-system
      interval: 10m
  interval: 10m
  dependsOn:
    - name: commoncal
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  rollback:
    timeout: 5m0s
  uninstall:
    keepHistory: false
  values:
    replicaCount: 1
    domain: mcal.hajnal.space
    bindAddress: "0.0.0.0:3001"
    image:
      repository: ghcr.io/david-hajnal/calendar-mcp
      tag: "sha-9db9f85457809b81daf0c767caf03f88d8f4c67e"
      pullPolicy: IfNotPresent
    ingress:
      enabled: true
      className: traefik
      annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
      hosts:
        - host: mcal.hajnal.space
          paths:
            - path: /mcp
              pathType: Prefix
            - path: /.well-known/oauth-protected-resource
              pathType: Exact
      tls:
        - secretName: commoncal-tls
          hosts:
            - mcal.hajnal.space
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 500m
        memory: 256Mi
    existingSecret:
      name: commoncal-mcp-secrets
      apiKeyKeyName: mcp-internal-api-key
      sessionSecretKeyName: mcp-session-secret
      # Primary OAuth issuer (unchanged — no cutover).
      oauthIssuerKeyName: mcp-oauth-issuer
      # New issuer to HOLD (accept for validation) without making it primary.
      # The value is the issuer of the commoncal-auth authorization server.
      # Set to empty to disable holding.
      oauthIssuerHoldKeyName: mcp-oauth-issuer-hold
    env:
      APP_ENV: production
      MCP_INTERNAL_API_BASE: https://cal.hajnal.space
      MCP_DOMAIN: mcal.hajnal.space
      MCP_PUBLIC_RESOURCE_URL: https://mcal.hajnal.space/mcp
      MCP_RATE_LIMIT_ENABLED: "1"
      TZ: Europe/Budapest
