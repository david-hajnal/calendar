apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: commoncal-mcp
  namespace: flux-system
spec:
  releaseName: commoncal-mcp
  targetNamespace: commoncal
  chart:
    spec:
      chart: ./deploy/helm/commoncal-mcp
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: flux-system
      interval: 10m
  interval: 10m
  dependsOn:
    - name: commoncal
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  rollback:
    timeout: 5m0s
  uninstall:
    keepHistory: false
  values:
    replicaCount: 1
    domain: mcal.hajnal.space
    bindAddress: "0.0.0.0:3001"
    image:
      repository: ghcr.io/david-hajnal/calendar-mcp
      tag: "sha-2a96ec716945e9d7fdc5303a1bd232d7cfa703bc"
      pullPolicy: IfNotPresent
    ingress:
      enabled: true
      className: traefik
      annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
      hosts:
        - host: mcal.hajnal.space
          paths:
            - path: /mcp
              pathType: Prefix
            - path: /.well-known/oauth-protected-resource
              pathType: Exact
      tls:
        - secretName: commoncal-tls
          hosts:
            - mcal.hajnal.space
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 500m
        memory: 256Mi
    networkPolicy:
      enabled: true
      ingress:
        from:
          - namespaceSelector:
              matchLabels:
                kubernetes.io/metadata.name: kube-system
    existingSecret:
      name: commoncal-mcp-secrets
      apiKeyKeyName: mcp-internal-api-key
      sessionSecretKeyName: mcp-session-secret
      # Primary OAuth issuer (unchanged — no cutover).
      oauthIssuerKeyName: mcp-oauth-issuer
    env:
      APP_ENV: production
      MCP_INTERNAL_API_BASE: https://cal.hajnal.space
      MCP_DOMAIN: mcal.hajnal.space
      MCP_PUBLIC_RESOURCE_URL: https://mcal.hajnal.space/mcp
      MCP_RATE_LIMIT_ENABLED: "1"
      TZ: Europe/Budapest
