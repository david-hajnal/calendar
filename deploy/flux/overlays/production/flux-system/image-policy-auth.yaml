# Flux image policy for calendar-auth
# Selects the 'main' tag. The production label scopes this policy to the
# production ImageUpdateAutomation.
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: image-policy-auth
  namespace: flux-system
  labels:
    image.fluxcd.io/production: "true"
spec:
  imageRepositoryRef:
    name: image-repository-auth
  filterTags:
    pattern: '^main$'
    extract: 'main'
  policy:
    alphabetical: {}
