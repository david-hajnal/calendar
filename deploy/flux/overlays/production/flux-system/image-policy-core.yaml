# Flux image policy for calendar-core — selects newest semver tag
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: image-policy-core
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: image-repository-core
  policy:
    semver:
      range: ">=0.0.0"
  sort: desc
