{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "commoncal.fullname" . }}
  labels:
    {{- include "commoncal.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "commoncal.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: TCP
          port: 443
    - ports:
        - protocol: TCP
          port: 53
        - protocol: UDP
          port: 53
    {{- if .Values.authBridge.enabled }}
    # Authorization bridge (commoncal-auth internal service). The bridge key
    # (AUTH_BRIDGE_KEY) authenticates every call; this egress rule only permits
    # the network path. Both the Service port (80) and the container port
    # (4001) are allowed to be robust across CNI DNAT ordering.
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Release.Namespace }}
          podSelector:
            matchLabels:
              app.kubernetes.io/component: authorization
      ports:
        - protocol: TCP
          port: 80
        - protocol: TCP
          port: 4001
    {{- end }}
  ingress:
    - ports:
        - protocol: TCP
          port: http
      {{- with .Values.networkPolicy.ingress.from }}
      from:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
