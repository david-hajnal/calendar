{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "replicaCount": {
      "description": "SQLite-backed CommonCal is a single-replica deployment.",
      "type": "integer",
      "const": 1
    },
    "existingSecret": {
      "type": "object",
      "required": ["name", "sessionSecretKey"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "sessionSecretKey": { "type": "string", "minLength": 1 }
      }
    },
    "mcpInternalApiSecret": {
      "type": "object",
      "description": "Optional Secret mapping for the core MCP internal API key.",
      "required": ["name", "key"],
      "properties": {
        "name": { "type": "string" },
        "key": { "type": "string", "minLength": 1 }
      }
    },
    "persistence": {
      "type": "object",
      "required": ["size"],
      "properties": {
        "size": { "type": "string", "minLength": 1 }
      }
    }
  }
}
